{
  "name": "avi-config-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file, client-side analyzer for Avi (NSX ALB) controller config exports. The app is avi-analyzer.html; this package.json exists only to run the tests.",
  "type": "module",
  "scripts": {
    "test": "node --test tools/tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
